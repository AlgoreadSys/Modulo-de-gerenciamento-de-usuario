name: ASP.NET Core Tests

on:
  push:
    branches:
      - test
  pull_request:
    branches:
      - test

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0'

      - name: Install dependencies
        run: |
          cd "src"
          dotnet restore "DotNet.Docker.csproj"

      - name: Build
        run: |
          cd "src"
          dotnet build "DotNet.Docker.csproj" --no-restore --configuration Release

      - name: Run tests
        run: |
          cd "UnitTests"
          dotnet test "UnitTests.csproj" --no-build --verbosity normal
